@{
    ViewData["Title"] = "Generera 2FA-återställningskoder";
    ViewData.AddActivePage(ManageNavPages.TwoFactorAuthentication);
}

<h2>@ViewData["Title"]</h2>

<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Detta genererar nya återställnignskoder.</strong>
    </p>
    <p>
        Om du förlorar din mackapär och inte har återställningskoder kommer du att förlora tillgång till ditt konto.
    </p>
    <p>
        Genom att avaktivera 2FA ändras inte några nycklar i dina autentiseringsapplikationer. Om du önskar att ändra nyckeln som används i en autentiseringsapplikation bör du <a asp-action="ResetAuthenticatorWarning">återställa dina autentiseringsnycklar.</a>
    </p>
</div>

<div>
    <form asp-action="GenerateRecoveryCodes" method="post" class="form-group">
        <button class="btn btn-danger" type="submit">Generera återställningskoder</button>
    </form>
</div>
